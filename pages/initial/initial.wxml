
<cu-custom bgColor="bg-gradual-pink" isCustom="{{true}}">
	<view slot="backText">返回</view>
	<view slot="content">分享比赛</view>
</cu-custom>


 <view class="bg-img bg-mask padding-tb-xl" style="background-image: url('../../images/wzry1.jpg');height: 50%;" bindtap="openCurrentGame" data-type="default-wzry"
 data-gid="{{gameinfo.id}}" data-iscreater="{{gameinfo.creater == openid}}" data-gname="{{gameinfo.gamename}}" data-pattern="{{gameinfo.pattern}}">
    <view class="padding-xl text-white">
      <view class="padding-xs text-xl">比赛 ： {{gameinfo.gamename}}</view>
      <view class="padding-xs">简介 ： {{gameinfo.describe}}</view>
      <view class="padding-xs">对战模式 : {{gameinfo.pattern?0:'5v5 个人赛'}}</view>
      <view class="padding-xs">结束时间 ：{{gameinfo.etime}}</view>

<!-- BUG后期处理 头像多了没有换行  -->
      <view class='index'>已报名：
        <view wx:for="{{participants}}" wx:key  style='transform:translateX({{-index*30}}rpx)'>
           <image class='text' src='{{item.avatarurl}}' mode='aspectFill'></image>
        </view>
        <text ></text>
      </view>
    </view>
  </view>
      <view class="bg-white padding padding-top-xl padding-bottom-xxl flex-sub">
       <button wx:if="{{hasUserInfo}}"  open-type='share' >分享</button>
       <button wx:if = "{{!hasUserInfo}}"  open-type='getUserInfo' bindgetuserinfo='bindgetuserinfo' >登录参与比赛</button>
    </view>
